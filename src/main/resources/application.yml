server:
  port: 8081

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/blog
    driver-class-name: org.mariadb.jdbc.Driver
    username: cos
    password: cos1234
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update